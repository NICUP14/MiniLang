import stdlib.io.read
import stdlib.io.print
import src.regex

fun main: int32
    _print_sep = ", "
    print(1, 2, 3, 4)
    # let pattern = input()
    # let from = input()
    # let to = input()
    # let matchlength: c_int = 0

    # match(pattern, text).print
    # match(pattern, text).get_idx.print
    # re(pattern).search(text).print

    # let pattern = "ow"
    # let from = "Hellowgow"
    # let to = "wu"
    # match(pattern, from).found.print
    # fast_replace(pattern, from, to).println
end
